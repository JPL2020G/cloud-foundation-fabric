# skip boilerplate check

# yaml-language-server: $schema=../../../schemas/subnet.schema.json

name: sb-use1-landing-prod
region: primary
ip_cidr_range: 10.239.240.0/24
description: Default primary-region subnet for landing in use1