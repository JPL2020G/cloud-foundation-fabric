# skip boilerplate check

# yaml-language-server: $schema=../../../schemas/subnet.schema.json

name: rd-gcp-poc-net-spoke-0
region: primary
ip_cidr_range: 10.240.0.0/18 # Check with customer
description: Default primary-region subnet for POC
